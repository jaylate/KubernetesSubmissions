apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: project
  name: todo-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: todo-app
  template:
    metadata:
      labels:
        app: todo-app
    spec:
      volumes:
        - name: image-storage
          persistentVolumeClaim:
            claimName: todo-app-claim
      containers:
      - name: todo-app
        image: todo-app
        imagePullPolicy: IfNotPresent
        resources:
          requests:
            cpu: "20m"
            memory: "50Mi"
          limits:
            cpu: "200m"
            memory: "100Mi"
        readinessProbe:
          initialDelaySeconds: 10
          periodSeconds: 5
          httpGet:
             path: /healthz
             port: 8080
        livenessProbe:
          initialDelaySeconds: 20
          periodSeconds: 5
          httpGet:
             path: /healthz
             port: 8080
        volumeMounts:
          - name: image-storage
            mountPath: /app/images
        env:
          - name: PORT
            valueFrom:
              configMapKeyRef:
                name: todo-app-conf
                key: default-port
          - name: ALLOWED_IMAGE_AGE
            value: "600"
          - name: BACKEND_URL
            value: "http://todo-backend-svc:8080/todos"
          - name: BACKEND_HEALTH_URL
            value: "http://todo-backend-svc:8080/healthz"
