<!DOCTYPE html>
<html>
  <head>
    <title>The project app</title>
  </head>
  <body>
    <h1>The project App</h1>
    <img src="<%= imagePath %>" width="400px" height="auto" />
    <form action="<%= backendURL %>" method="POST">
      <input name="todoText" maxlength="140" />
      <button>Create todo</button>
    </form>
    <ul>
      <% todos.forEach((item) => { %>
        <li><%= item %></li>
      <% }); %>
    </ul>
    <p>DevOps with Kubernetes 2025</p>
  </body>
</html>
